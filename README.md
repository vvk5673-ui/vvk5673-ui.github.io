# Превью сайта КровМастер

Служебный репозиторий только для постоянной HTTPS-ссылки на превью сайта
(GitHub Pages user site — публикуется от корня домена).

Не боевой сайт, не основной репозиторий проекта. Основной код и история —
в приватном репозитории `vvk5673-ui/krov_master`, ветка `main`, папка `site/`.

Обновление: содержимое этого репозитория перезаливается целиком из `site/`
основного проекта, без сохранения истории правок здесь.
